# Imported Claude Agents

This directory contains sanitized OpenClaw-compatible wrappers for the imported Claude agent catalog.

- Runtime id pattern: `claude-<source-file-slug>`
- Count in this snapshot: 147
- Source catalog: `departments/agent-catalog/claude-agents-catalog.md`
- Delegation runbook: `departments/runbooks/claude-agent-delegation.md`

These are template profiles only. Local absolute paths have been replaced with placeholders such as `{{OPENCLAW_HOME}}`, `{{CLAUDE_HOME}}`, and `{{HOME}}`.
